# ========== 애플리케이션 기본 설정 ==========
spring.application.name=market-map

# ========== 데이터베이스 설정 ==========
# spring.datasource.url=jdbc:mysql://localhost:3306/market_map?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
# spring.datasource.username=root
# spring.datasource.password=topy1004
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========== 운영 데이터베이스 설정 ==========
spring.datasource.url=jdbc:mysql://zetta-system.iptime.org:53306/market_map?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=topy2u2018
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# JPA 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# 파일 업로드 설정
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=20MB
spring.servlet.multipart.enabled=true

# JSP 설정
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# 정적 리소스 설정 (WebConfig.java에서 처리)
spring.web.resources.add-mappings=true

# 로깅 설정
logging.level.com.market_map=DEBUG
logging.level.org.springframework.security=DEBUG
logging.file.name=logs/market-map.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# 서버 설정
server.port=8080
server.servlet.context-path=/

# 애플리케이션 설정
app.name=관문상가시장 관리 시스템
app.version=1.0.0
app.upload-dir=uploads/stores
app.map-image-path=/static/images/map.png
